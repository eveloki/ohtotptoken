/**
 * Copyright (c) Huawei Technologies Co., Ltd. 2023 All rights reserved.
 */
import * as agconnect from "../../common-server";
export declare class FunctionCallable {
    private agcClient;
    private requestTemplate;
    private GWUrlFrontMiddle;
    private httpTrigger;
    private httpClient;
    /**
     * maxBodyLength http请求body体最大长度
     * maxContentLength http接收体result最大长度
     * timeout 超时时长
     *
     * @protected
     */
    private httpClientConfig;
    /**
     * 初始化构造
     *
     * @param agcClient 认证实例
     * @param httpTrigger 被调函数名加版本号
     */
    constructor(agcClient: agconnect.AGCClient, httpTrigger: string);
    private static getCloudGWUrlFront;
    private generateToken;
    /**
     * 云函数调用请求
     *
     * @param data 调用传参
     * @return 完整函数调用返回结果
     */
    call(data?: any): Promise<any>;
    /**
     * 设置调用云函数的超时时间
     * @param timeout 超时时长，单位ms
     */
    setTimeout(timeout: number): void;
}
