/**
 * Copyright (c) Huawei Technologies Co., Ltd. 2023 All rights reserved.
 */
import { FunctionCallable } from "./FunctionCallable";
export declare class AGCFunction {
    private readonly agcClient;
    /**
     * 初始化构造
     *
     * @param name 实例名
     */
    constructor(name?: string);
    /**
     * 设置被调用云函数的HTTPTrigger
     *
     * @param functionName 函数名
     * @param version 版本号
     */
    wrap(functionName: string, version: string): FunctionCallable;
}
