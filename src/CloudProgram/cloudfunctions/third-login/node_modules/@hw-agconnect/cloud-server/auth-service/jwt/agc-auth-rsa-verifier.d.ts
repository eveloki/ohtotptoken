import { AGCClient } from "../../common-server";
export declare class AGCAuthRsaVerifier {
    static verify(publicKey: string, token: string, alg: string, client: AGCClient): any;
    private static getSub;
    private static toBase64;
}
