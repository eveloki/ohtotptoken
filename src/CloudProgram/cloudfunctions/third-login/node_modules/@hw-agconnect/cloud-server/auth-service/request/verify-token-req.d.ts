/**
 * Copyright (c) Huawei Technologies Co., Ltd. 2023 All rights reserved.
 */
import { RequestConstructor, AGCClient } from "../../common-server";
export declare class VerifyTokenReq implements RequestConstructor {
    private readonly url;
    private client;
    private accessToken;
    constructor(accessToken: string, client: AGCClient);
    getUrl(useBackUrl?: boolean): string;
    getHeader(): Promise<any>;
    getBody(): any;
}
