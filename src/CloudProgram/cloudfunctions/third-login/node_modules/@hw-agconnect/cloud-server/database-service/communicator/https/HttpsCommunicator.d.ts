/**
 * Copyright (c) Huawei Technologies Co., Ltd. 2023 All rights reserved.
 */
import * as agconnect from '../../../common-server';
export declare class HttpsCommunicator {
    private maxBodyLength;
    private maxContentLength;
    private fiveMinutesMs;
    private timeout;
    private requestTemplate;
    private agcClient;
    private httpClient;
    private requestPort;
    private readonly WAIT_SEND_MS;
    private readonly POST;
    private readonly GET;
    private readonly PUT;
    private readonly DELETE;
    private readonly HTTP_STATUS_OK;
    private readonly RETRY_PROCESS_REQUEST_COUNT;
    constructor(agcClient: agconnect.AGCClient);
    /**
     * This method is used to send http get request.
     *
     * @param requestUrl the url request is sent to
     * @param requestId the requestId
     * @param useBackup use backup gw url
     */
    sendGetRequest(requestUrl: string, requestId: string, useBackup?: boolean | undefined): Promise<any>;
    /**
     * This method is used to send http post request.
     *
     * @param requestUrl the url request is sent to
     * @param requestId the requestId and traceId
     * @param data body of the request
     * @param useBackup use backup gw url
     */
    sendPostRequest(requestUrl: string, requestId: any, data: any, useBackup?: boolean | undefined): Promise<any>;
    /**
     * This method is used to send http delete request.
     *
     * @param requestUrl the url request is sent to
     * @param requestId the requestId and traceId
     * @param data the body of the request
     * @param useBackup use backup gw url
     */
    sendDeleteRequest(requestUrl: string, requestId: any, data: any, useBackup?: boolean | undefined): Promise<any>;
    /**
     * This method is used to send http put request.
     *
     * @param requestUrl the url request is sent to
     * @param requestId the requestId and traceId
     * @param data the body of the request
     * @param useBackup use backup gw url
     */
    sendPutRequest(requestUrl: string, requestId: any, data: any, useBackup?: boolean | undefined): Promise<any>;
    private sendRequest;
    private generateAuth;
    /**
     * This method is used to send process request for batch operator.
     *
     * @param requestUrl the url request is sent to
     * @param requestId the requestId and traceId
     * @param data the body of the request
     * @param useBackup use backup gw url
     */
    sendProcessRequest(requestUrl: string, requestId: any, data: any, useBackup?: boolean | undefined): Promise<any>;
    private waitAsync;
}
